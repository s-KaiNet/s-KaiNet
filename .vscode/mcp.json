{
    "inputs": [
        {
            "id": "parallel-api-key",
            "type": "promptString",
            "description": "Parallel.io API Key",
            "password": true
        }
    ],
    "servers": {
        "parallel-search": {
            "type": "http",
            "url": "https://search-mcp.parallel.ai/mcp",
            "headers": {
                "Authorization": "Bearer ${input:parallel-api-key}"
            }
        }
    }
}